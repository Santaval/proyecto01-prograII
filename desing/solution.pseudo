struct safeTerrain {
  coordenates[][]
  size 
}

function replace(terrain, safeTerrain, biggestTerrain) {
  if (safeTerrain.size == biggestTerrain){
    for (safeTerrain.coordenates.length) {
      terrain[safeTerrain.coordenates[i]][safeTerrain.coordenates[j]] = 'R'
    }
  }
  return terrain
}